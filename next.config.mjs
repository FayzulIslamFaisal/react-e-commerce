/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["upload.wikimedia.org", "flagcdn.com", "assets.adidas.com"], // Add any other domains you need here
  },
};

export default nextConfig;
